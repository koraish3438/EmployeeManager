<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher_new"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_new_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.EmployeeManagementApp">
        <activity
            android:name=".ui.EmployeeListActivity"
            android:exported="false" />
        <activity
            android:name=".registration.SignupActivity"
            android:exported="false" />
        <activity
            android:name=".registration.LoginActivity"
            android:exported="false" />
        <activity
            android:name=".registration.WelcomeActivity"
            android:exported="false" /> <!-- Splash Screen -->
        <activity
            android:name=".splash.SplashActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity> <!-- Main Activity -->
        <activity
            android:name=".ui.AddEditEmployeeActivity"
            android:exported="false" /> <!-- Employee Detail -->
        <activity
            android:name=".ui.EmployeeDetailActivity"
            android:exported="false" />
    </application>

</manifest>